<!DOCTYPE html>
<html>
<head>
  <!-- theres a bit of code here thats not properly indented. -->
  <title>SimplyMommy</title>
   <link href='https://fonts.googleapis.com/css?family=Raleway:400,500|Poiret+One|Yesteryear' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header class="navbar navbar-fixed-top navbar-inverse">
    <div class="container">
      <%= link_to "SimplyMommy", root_path, id: "logo" %>
      <nav>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Home", root_path, {style: "color: #fff"} %></li>
          <li><%= link_to "Posts", posts_path, {style: "color: #fff"}%></li>

          <% if current_user %>
          Logged as: <%= current_user.email %>
          <li><%= link_to "Log Out", signout_path, {style: "color: #fff"}%></li>
          <% else %>
          <li><%= link_to "Log In", signin_path, {style: "color: #fff"}%></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </header>


  <div class="container">
    <% flash.each do |name, msg| -%>
      <%= content_tag :div, msg, class: "alert alert-#{name}" %>
    <% end %>

    <%= yield %>
</div>

</body>
</html>
